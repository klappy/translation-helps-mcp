# Netlify Configuration for Translation Helps API

[build]
  functions = "netlify/functions"
  publish = "."
  # Set API_VERSION from package.json during build
  command = "export API_VERSION=$(node -p \"require('./package.json').version\") && echo \"API_VERSION=$API_VERSION\" >> .env && npm run build"

[build.environment]
  NODE_VERSION = "18"

# Redirect /api/* to functions
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
  force = true

# CORS headers for API endpoints
[[headers]]
  for = "/api/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"
    Access-Control-Allow-Methods = "GET, POST, OPTIONS"
    Access-Control-Allow-Headers = "Content-Type, Authorization"
    Cache-Control = "public, max-age=300"

# Function-specific configuration
[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild" 